import styles from './page.module.scss';

const Essays = () => {
  return (
    <main className={styles.pageContainer}>
      <div className={styles.essayContainer}></div>
    </main>
  );
};

export default Essays;
